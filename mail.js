/**
 * Created by jinshang on 14-12-23.
 */
function Mail(config) {
    this.config = config;
    this.send = function(params, completion) {
        var api = 'https://api.submail.cn/mail/send.json';
        var requestParams = parmas;
    }
};

module.exports = Mail;